const [a, b, c, d] = [17, 7, 9, 4]

const soma = a + b + c + d
const subtracao = a - b - c - d
const multiplicacao = a * b * c * d
const divisao = a / b / c / d
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo) // 34 -14 5040 0.17857142857142858 0
